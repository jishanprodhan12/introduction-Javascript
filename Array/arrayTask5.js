/**
 * ### 5. Combining Arrays

*           *Instructions:**

1. Create two arrays of your choice.
2. Use the concat method to combine the two arrays into a new array.
3. Print both the original arrays and the combined array using console.log().
 */

    const array_1 = ['raihan' , 'niloy' , ' jishan' ];
    const array_2 = ['rasel',' sakib'];
    const finalAarry = array_1.concat(array_2);
    console.log(array_1);
    console.log(array_2);
    console.log(finalAarry);
